#include "bits/stdc++.h"
using namespace std;

int main()
{
    int n, m, k;
    cin >> n >> m >> k;
    int arr[n];
    int brr[m];
    for (int i = 0; i < n; i++)
    {
        cin >> arr[n];
    }
    for (int i = 0; i < m; i++)
    {
        cin >> brr[i];
    }
    for (int i = 0; i < n; i++)
    {
    }
}
